# ===============================
# 🔧 Alkalmazás beállítások
# ===============================
spring.application.name=backend
server.port=8080

# ===============================
# 🗄️ Adatbázis (PostgreSQL)
# ===============================
# Railway / Hosting
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/autokereskedes}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# 🔐 Biztonság / JWT
# ===============================
# Hosting esetén a SECRET az ENV-ből jön, lokálisan a fallback érték működik
app.jwt.secret=${JWT_SECRET:ThisIsASuperSecretKeyThatIsAtLeast32CharsLong!!!}
app.jwt.expiration-millis=86400000

# ===============================
# 🌐 CORS engedélyezés
# ===============================
spring.web.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=Authorization,Content-Type
spring.web.cors.allow-credentials=true


app.mail.from=${APP_MAIL_FROM}
